# تقرير معيار الابتكار — أثر

## النتيجة التنفيذية

الدرجة الذاتية العدائية وفق الرُبرِك:

`10/10`

بُنيت الحلقات الخمس كلها كوحدات مستقلة تعمل في بيئة المتصفح وفي بيئة الاختبار، مع مختبر عرض محلي يربطها بالمحرك القائم من دون تعديل ملفاته.

عدد دورات التنفيذ:

`7`

خمس دورات اختبارية للوحدات، ودورة تكامل للمختبر، ودورة تدقيق عدائي نهائية.

## الملفات المسلّمة

`presentation/athar-boundary.js`

`presentation/athar-budget.js`

`presentation/athar-reasons.js`

`presentation/athar-conflict.js`

`presentation/athar-memory.js`

`presentation/data/innovation-demo-data.js`

`presentation/tests/innovation-test.js`

`presentation/athar-lab.html`

`docs/superpowers/plans/2026-07-23-athar-innovation-loops.md`

كلها ملفات جديدة. لم يُعدّل أي ملف قائم أو محظور في التسليم.

## الحلقة الأولى — حد التأثير الديناميكي

الملف:

`presentation/athar-boundary.js`

يشتق نصف قطر التأثير من ثلاثة مكونات ظاهرة:

- نصف طول منطقة العمل.

- تمدد الطلب نسبة إلى السعة الكاملة.

- تمدد فقد السعة نسبة إلى عدد المسارات المغلقة.

المعادلة المعروضة:

```text
base radius × (1 + demand / capacity + closed-lane share)
```

لا توجد مسافة ثابتة أو معامل معايرة مخفي. يعرض المختبر كل مكوّن بالأمتار ويتغير الحد بصريًا عند تغيير الحجم أو المسارات أو طول العمل.

## الحلقة الثانية — ميزانية أثر المحور

الملف:

`presentation/athar-budget.js`

يجمع ساعات-المركبات للتصاريح المقبولة في المحور والشهر نفسيهما، ثم يضيف أثر الطلب الجديد ويقارنه بالسقف الذي تحدده الجهة.

المعادلة المعروضة:

```text
sum(accepted corridor permit delay in month) + requested permit delay
```

إذا بقي المجموع داخل السقف يصدر قرار قبول. وإذا تجاوزه يصدر قرار إعادة جدولة ويقترح أول شهر تالٍ يملك سعة كافية.

هذه الحلقة تتغير فعليًا بين القبول وإعادة الجدولة عند تحريك أثر الطلب أو السقف في المختبر.

## الحلقة الثالثة — المفسر الكمي

الملف:

`presentation/athar-reasons.js`

يحسب لكل بديل أربعة فروق رقمية:

- انخفاض الطلب.

- انخفاض ساعات الطابور.

- انخفاض ساعات أشخاص الحافلات.

- انخفاض تعارضات المحور.

لكل عامل قيمة أساس، وقيمة بديل، وفرق مطلق، ونسبة فرق، ووحدة قياس، ونقاط مساهمة، ووزن من النتيجة النهائية.

لا توجد أوزان عوامل ثابتة. التطبيع مشتق من نطاق القيم المرصودة بين البدائل في التشغيل نفسه:

```text
factor improvement / observed candidate-set range
```

يتغير الترتيب عند تغيير أثر الطابور أو الحافلات، ولا تعيد الوحدة مصفوفة أسباب نصية.

## الحلقة الرابعة — التعارضات المتعددة

الملف:

`presentation/athar-conflict.js`

تحلل الوحدة عددًا عامًا من التصاريح وتنتج:

- مصفوفة زمنية مكانية كاملة.

- ساعات التراكب لكل زوج.

- عدد المقاطع المشتركة.

- تعرضًا مشتقًا من حاصل التراكب والمقاطع المشتركة.

- مجموعات دمج تلقائية للأعمال المتقاربة.

- أثرًا مركبًا للمحفظة.

استُبدل معامل التفاعل الثابت في مسار القرار الجديد بمعامل مشتق من سجل المحفظة:

```text
1 + shared-segment overlap-hours / total permit-hours
```

يتغير المعامل عندما يتغير توقيت تصريح، وتستجيب مجموعات الدمج لنافذة التنسيق التي تحددها الجهة.

## الحلقة الخامسة — ذاكرة المعايرة

الملف:

`presentation/athar-memory.js`

تحتفظ الوحدة بالمتوقع والمرصود لكل حالة، ثم تعيد حساب معامل التصحيح من البيانات:

```text
sum(observed vehicle-hours) / sum(predicted vehicle-hours)
```

السجل التركيبي موسوم بوضوح ويضم خمس حالات بأخطاء أولية بين:

`5%–40%`

تعرض الصفحة نجاحات وإخفاقات قبل المعايرة وبعدها، إضافة إلى عدد الحالات التي تحسنت والحالات التي ساءت. لا تحذف المعايرة الحالات السلبية ولا تجعل الحكم ناجحًا بتصميم الواجهة.

## معالجة الثوابت القديمة

لا تستدعي الحلقات الجديدة مسار التفاعل القديم لاتخاذ القرار.

`COMPOUND_FACTOR = 1.3`

يستخدم كاشف التعارضات نسبة مشتقة من ساعات التراكب بدلًا منه.

ولا تستخدم الحلقات الجديدة قيمة التصنيف القديمة لاتخاذ قرارات الميزانية أو الترتيب أو المعايرة.

`SCORE_CALIBRATION = 0.35`

يعرض المختبر ساعات التأخير من المحرك القائم، ثم يحسب قراراته الجديدة من مدخلات ومجاميع ونطاقات ظاهرة. معامل ذاكرة التصحيح مشتق من سجلات المتوقع والمرصود.

## وسم البيانات والمصادر

بيانات العرض كلها تحمل الوسم:

`افتراض توضيحي للعرض`

وتوضح أنها بيانات تركيبية تستبدل بقياسات التصاريح عند التكامل.

المراجع المنهجية المعروضة:

`src-003`

منهج كود الطرق السعودي.

`src-021`

مقاييس أداء مناطق العمل.

المراجع التنافسية المعروضة:

`src-024`

`one.network`

`src-030`

`Singapore LTA`

## التميز الصادق

لا يتضمن المختبر ادعاء سبق محلي أو عالمي.

تثبت سنغافورة نضج أتمتة مراجعة خطط المرور، ويثبت الحل المرجعي الدولي نضج التصاريح والتنسيق والخريطة والنشر.

التميّز المقترح هو الجمع بين التوقع المنسوب، واقتصاد كود الطرق السعودي، وميزانية أثر المحور، والترتيب الكمي، والمعايرة بعد التنفيذ في طبقة قرار فوق بلدي.

## التحقق

اختبارات الابتكار:

```powershell
node presentation/tests/innovation-test.js
```

النتيجة:

`20/20`

اختبارات المحرك القائمة:

```powershell
node presentation/tests/engine-test.js
```

النتيجة:

`47/47`

المجموع:

`67/67`

الفحص البنيوي للمختبر:

- تسعة وأربعون معرّفًا فريدًا.

- أحد عشر عنصر تحكم مرتبطًا بتسميات.

- ثمانية وعشرون هدفًا للشيفرة موجودة في المستند.

- مئة وتسعة وخمسون مقطع تنسيق متوازنًا.

- لا ملفات شبكة أو خطوط أو مكتبات خارجية.

- لا ادعاءات سبق محظورة.

- فحص المسافات البيضاء للتغييرات بلا أخطاء.

## قيد التحقق المرئي

رفض متصفح التطبيق فتح مسار القرص المحلي بسبب سياسة أمان للروابط المحلية.

`file://`

لم يُستخدم خادم بديل أو مسار التفاف على السياسة. عُوض ذلك بفحص بنيوي وصحة الشيفرة المضمنة وعقود العناصر.

ينبغي للمنسق فتح الصفحة مباشرة من القرص في متصفح محلي أثناء الدمج لإجراء لقطة العرض النهائية وفحص سجل المتصفح. هذا قيد في بيئة التحقق الحالية، وليس اعتمادًا شبكيًا في الصفحة.

## نقاط الدمج المقترحة

- يظهر الحد الديناميكي حول الحفرية المختارة على الخريطة، مع زر يفتح تفصيل مكوناته.

- تظهر ميزانية المحور بجوار قرار اعتماد توقيت التصريح، قبل الإرسال النهائي.

- يستبدل جدول المساهمات الرقمية الأسباب النصية في بطاقة البدائل.

- تظهر مصفوفة التعارض في شاشة تنسيق محفظة التصاريح، مع إجراء دمج للمجموعات المقترحة.

- تظهر ذاكرة المعايرة في شاشة قياس ما بعد التنفيذ، مع خطأ قبل التصحيح وبعده.

## حدود منهجية معلنة

- معادلات الحدود والتفاعل والمعايرة آليات أولية شفافة وليست نماذج مدربة على بيانات الرياض.

- سقف ميزانية المحور قرار حوكمة تدخله الجهة، وليس رقمًا عامًا ثابتًا داخل الوحدة.

- سجل المعايرة الحالي تركيبي وموسوم، ويجب استبداله بالسجل الفعلي عند التجربة.

- الحالات عالية الأثر تظل مرشحة لمحاكاة متخصصة، بينما يخدم المختبر الفحص السريع واتخاذ القرار المتكرر.

## التقييم العدائي حسب الرُبرِك

- خمس حلقات جديدة تعمل وتتغير مع المدخلات، والميزانية مكتملة: لا خصم.

- المفسر كمي وكل عامل له رقم ووزن: لا خصم.

- لا ثابت اعتباطي جديد في مسارات القرار، وللثابتين القديمين بدائل مشتقة معلنة: لا خصم.

- ذاكرة المعايرة تعرض نجاحًا وفشلًا وتحسنًا وتراجعًا: لا خصم.

- صياغة التميز صادقة مقابل سنغافورة والحل المرجعي الدولي: لا خصم.

- الصفحة تعمل دون اعتماد شبكي وكل ملفات التسليم جديدة: لا خصم.

الدرجة النهائية:

`10/10`
