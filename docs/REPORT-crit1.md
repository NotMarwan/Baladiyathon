# REPORT — المعيار 1: المسارات البديلة + التنبؤ بالازدحام

**الفرع:** `crit1-challenge` · **الدورات:** 3 · **الدرجة الذاتية العدائية: 9.3/10**

## ملخص

شطرا «المسار البديل» و«التنبؤ» كانا شكليين (خط بإزاحة ثابتة `0.006°` + مصفوفة 24 قيمة يدوية). أُعيد بناؤهما على حساب فعلي: شبكة طرق محلية (OSM/ODbL) + Dijkstra/BPR للمسارات، وطبقة تنبؤ صادقة بنطاق عدم يقين ومسار معايرة CSV. كل الاختبارات تنجح (47 محرك + 18 توجيه/تنبؤ)، صفر أخطاء console عبر HTTP و`file://`.

## التغييرات (ملف:سطر)

| ملف | التغيير |
|---|---|
| `presentation/data/corridor-network.json` (جديد) | شبكة 18 عقدة / 27 ضلعاً: الملك فهد + العليا + التخصصي + 6 تقاطعات عرضية. هندسة من OSM (ODbL)، قيم السعة/السرعة موسومة «افتراض توضيحي». POIs: مستشفى + مدرسة موسومان. |
| `presentation/athar-routing.js` (جديد) | `buildGraph` · `edgeTravelTime` (BPR) · `shortestPath` (Dijkstra) · `alternativeRoutes` (3 بدائل k-diverse بطريقة العقوبة) · `corridorThroughTime` (زمن البقاء على الممر) · `shockwave` (انتشار الطابور) · `haversineKm`. |
| `presentation/athar-forecast.js` (جديد) | `forecast` بنطاق ±15٪ (±8٪ بعد المعايرة) · `DOW_FACTORS` (عطلة الجمعة/السبت أخفض) · `calibrateFromCSV` (تحقق + تطبيع) · `forecastGrid` (24×5 للخريطة الحرارية). |
| `presentation/athar-prototype.html` | حذف `drawReroute` (الوهمي)؛ أُضيف `drawComputedRoutes`/`renderRouteCards`/`drawShockwave`/`renderHeatmap`/معالج CSV + بطاقتا «المسارات البديلة» و«التنبؤ». TMP يطبع أسماء الشوارع الفعلية + الدقائق. `NETWORK_FALLBACK` مضمّن للعمل بلا اتصال. |
| `presentation/tests/routing-test.js` (جديد) | 18 اختباراً — مستقل عن ملف اختبارات المحرك. |

## الدرجة بالدليل (رُبرِك عدائي، ابدأ من 10)

| فحص | خصم محتمل | الحالة | الدليل |
|---|---|---|---|
| مسار بديل محسوب على شبكة فعلية لا مرسوم | −3 | **0** | Dijkstra على 18 عقدة، أوزان BPR. مثال حي: kf_3 مغلق ذروة 3 مسارات: `kf0>kf1>kf2>tk2>tk3>kf3>kf4>kf5`. |
| زمن رحلة مقارن يتغير مع المدخلات | −2 | **0** | ذروة+3مسارات: البقاء 50 د مقابل البديل 9 د (يوفر 41). ذروة+2: يوفر 1 فقط. ليل+3: البديل أبطأ بـ2 (البقاء أفضل) — سلوك صادق عكوس للمدخلات. |
| تنبؤ بنطاق عدم يقين + وسم صادق + مسار معايرة | −2 | **0** | كل خلية تعرض «نطاق X–Y٪». وسم «تنبؤ نموذجي — يُعاير ببيانات المشغل». رفع CSV يقلّص النطاق إلى ±8٪ ويقلب الشارة. الثلاثاء 08:00=93٪ مقابل الجمعة=62٪. |
| مطالب التحدي الأربعة حية | −1.5 | **0** | تحليل (بطاقة الأثر) · توصيات (الجدولة المثلى) · مسارات بديلة (البطاقة الجديدة) · تنبؤ (الخريطة الحرارية) — كلها تفاعلية. |
| بلا اتصال + صفر أخطاء | −1 | **0** | `NETWORK_FALLBACK` + بوابة `navigator.onLine`. صفر أخطاء console عبر HTTP و`file://`. |
| تعميق جديد يعمل | −0.5 | **0** | تعميقان: `shockwave` (توهّج أحمر على المقاطع المجاورة، فائض 5000 مركبة/س ذروة) + فحص قرب POI (تنبيه «يمر قرب مستشفى — وصول حرج»). |
| **خصم ذاتي عدائي (شفافية)** | — | **−0.7** | الشبكة مبسّطة (18 عقدة يدوية من OSM لا استيراد كامل)؛ مواقع POI نقاط توضيحية؛ مسار المعايرة موجود لكن لا CSV حقيقي مُرفق؛ لقطة الشاشة تعذّرت في أداة المعاينة (Leaflet ثقيل) — تُحقّق النتائج عبر قراءة DOM وتقييم JS. |

**المجموع: 10 − 0.7 = 9.3**

## صدق الادعاءات (لملفات وكيل آخر — لم تُلمس)

`athar.html:112` «يقيس جدواها» و`athar-merged.html:167` «على الشبكة»: بعد هذا العمل صار المحرك يحسب فعلاً المسار البديل على شبكة (Dijkstra/BPR) ويقيس زمن رحلته وسعته المتبقية — الادعاءان صارا مدعومَين بكود. الملفات نفسها لم تُعدّل (حد الملكية).

## ما تعذّر

- استيراد OSM آلي كامل: استُبدل بشبكة يدوية مبسّطة موسومة المصدر (كافية لإثبات الحساب الفعلي؛ التوسّع مباشر).
- لقطة شاشة من أداة المعاينة: تعذّرت (المعاينة تعامل الملف كلقطة ساكنة عبر `file://`، وعبر HTTP يتجمّد التقاط Leaflet). كل التحقق تم بقراءة شجرة DOM وتقييم JS مباشر.

## التحقق

```
node presentation/tests/engine-test.js   → ALL TESTS PASSED (47)
node presentation/tests/routing-test.js  → 18 passed, 0 failed
متصفح (HTTP + file://)                    → 0 أخطاء console
```
