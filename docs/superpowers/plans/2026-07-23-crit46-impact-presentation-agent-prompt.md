# برومبت الأثر والاستدامة والعرض التقديمي

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:**

رفع معيار الأثر والاستدامة ومعيار العرض التقديمي فقط، عبر فصل الأثر المقاس عن التركيبي، وبناء بروتوكول ظل يمنع النجاح بلا أدلة، ثم مزامنة كل رقم وادعاء في العرض مع مصدره المباشر.

**Architecture:**

وحدة تقييم نقية لنتائج التشغيل الظلي، تستبعد الحالات التركيبية من قرار النجاح، وتنتج مقاييس خطأ ومنفعة ومخالفات قابلة للعرض؛ ثم عرض قصير يروي قراراً واحداً ويربط كل رقم بدليله.

**Tech Stack:**

`JavaScript`

`UMD`

`HTML`

`node:assert`

`Markdown`

**Global Constraints:**

لا تعدّل المحرك أو التوجيه أو الخادم أو النموذج الرئيسي. لا تفوّض العمل إلى وكلاء فرعيين. لا تسأل المستخدم. لا تخترع نتيجة ميدانية أو أعضاء فريق أو تكاملاً حكومياً. استخدم المصادر الأولية، وافصل الحقيقة عن الاستنتاج والافتراض، واكتب اختباراً يفشل قبل بناء وحدة تقييم الظل.

## البرومبت الجاهز للنسخ

انسخ النص من بداية السياج التالي إلى نهايته وأرسله كاملاً إلى الوكيل.

```text
أنت مسؤول عن معيارين فقط:

٤. الأثر والاستدامة.
٦. العرض التقديمي.

مهمتك تحويل الأثر من ادعاء حساس للافتراضات إلى بروتوكول إثبات يمكن أن يفشل، ثم جعل العرض وبطاقة الفكرة يقولان ما يثبته المنتج والمصادر فقط.

اعمل باستقلال كامل حتى التسليم. لا تسألني، ولا تنتظر رداً، ولا تنشئ وكلاء فرعيين. إذا لم توجد نتيجة ميدانية، لا تملأ الفراغ برقم تركيبي؛ اجعل نقص الأدلة نفسه حالة ظاهرة.

استخدم التشخيص المنهجي:

١. احصر كل ادعاء رقمي.
٢. تتبع مصدره المباشر.
٣. صنفه إلى مقاس أو رسمي أو مشتق أو افتراض أو غير مدعوم.
٤. أعد إنتاج التناقض بين العرض والملفات.
٥. اكتب فرضية واحدة عن سبب فقدان الثقة.
٦. اختبرها بإزالة أو تضييق ادعاء واحد.
٧. اكتب اختبارات تمنع رجوع الادعاءات القديمة.

مسار العمل:

C:\Users\wasan\Downloads\Swarm\Baladiyathon

ابدأ بقراءة هذه الملفات كاملة:

docs\FINAL-STRICT-EVALUATION.md
docs\REPORT-crit4.md
docs\FEASIBILITY.md
presentation\athar-pitch.html
presentation\athar.html
presentation\athar-merged.html
presentation\athar-sources.html
presentation\athar-impact-calibration.js
presentation\tests\engine-test.js
presentation\tests\ui-smoke-test.js
بطاقة-الفكرة.md

شغّل خط الأساس:

node presentation/tests/engine-test.js
node presentation/tests/ui-smoke-test.js
node presentation/tests/routing-test.js
node presentation/tests/innovation-test.js

حدود الملكية صارمة.

يمكنك إنشاء أو تعديل هذه الملفات فقط:

presentation\athar-shadow-evaluation.js
presentation\tests\shadow-evaluation-test.js
presentation\tests\pitch-integrity-test.js
presentation\athar-pitch.html
presentation\athar.html
presentation\athar-merged.html
presentation\athar-sources.html
بطاقة-الفكرة.md
docs\SHADOW-PILOT.md
docs\REPORT-crit46-final.md

لا تعدّل المحرك أو التوجيه أو الخادم أو النموذج الرئيسي أو ملفات الجدوى أو ملفات القرار الجديدة.

المشكلات المثبتة:

- لا توجد حالة ميدانية مقاسة تثبت الأثر.
- سجل المعايرة الحالي تركيبي، لذلك لا يجوز استخدامه لإعلان النجاح.
- نتيجة الأثر شديدة الحساسية للحركة والإشغال والوقود وقيمة الوقت.
- بطاقة الفكرة تحتوي ادعاء سبق عاماً غير قابل للدفاع.
- بطاقة الفكرة تحتوي تقديراً سنوياً يقارب عشرة ملايين ساعة مركبة وعشرين ألفاً وثمانمئة طن بلا مسار حساب موثق.
- بطاقة الفكرة توحي بعمل مباشر على بيانات بلدي من دون بنية جديدة، بينما التكامل الفعلي غير منفذ.
- وصف خوارزمية مستعمرة النمل لا يطابق التنفيذ.
- عداد العرض يذكر اثنتي عشرة شريحة بينما القصة الرئيسية تنتهي عند الشريحة الثامنة.
- بعض روابط الأرقام لا تفتح الدليل المباشر نفسه.
- بيانات الفريق ناقصة ولا يجوز اختلاقها.

مرحلة البحث الإلزامية:

ابحث في المصادر الرسمية الأولية الحالية عن الآتي:

GAO dig once savings 25 33 percent PDF
UK Department for Transport Street Manager two million works records
Balady road works interactive map official
Saudi Highway Code traffic management official PDF
RCRC open data bus and metro official

افتح المصدر نفسه، لا نتيجة البحث. خزّن في سجل المصادر:

- اسم الجهة.
- عنوان الوثيقة أو الصفحة.
- تاريخ النشر أو التحديث إن وجد.
- تاريخ الوصول.
- الرابط المباشر.
- الادعاء الذي يدعمه.
- ما لا يدعمه.

في حالة التوفير من الحفر المشترك، استخدم تقرير الجهة الرقابية الأولي الذي يذكر النطاق نفسه. لا تجعل مقالة ثانوية هي الدليل الرئيسي.

في حالة حجم سجلات أعمال الطرق البريطانية، اربط الرقم بصفحة رسمية تذكر الحجم مباشرة. إذا تغير الرقم المنشور، حدّث العرض إلى الرقم الحالي مع تاريخ الوصول، ولا تحافظ على الرقم القديم لإبقاء الاختبار أخضر.

لا تبحث عن رقم سنوي بديل للأثر المحلي. لا يوجد إذن بتحويل حالة توضيحية إلى أثر مدينة سنوي.

اكتب في التقرير فرضية الثقة:

إذا فصل العرض الحالات المقاسة عن التركيبية، ومنع حكم النجاح دون حد أدنى من الحالات المقاسة، وربط كل رقم بمصدر مباشر، فسوف يصبح الأثر قابلاً للتدقيق حتى قبل اكتمال التجربة الميدانية.

المهمة الأولى: وحدة تقييم التشغيل الظلي.

أنشئ:

presentation\athar-shadow-evaluation.js

يجب أن تصدّر:

evaluateShadowPilot
validateShadowCases

كل حالة تحتوي:

permitId
kind
predictedVehHours
observedVehHours
counterfactualVehHours
transitBreach
criticalAccessBreach

القيم المسموحة للنوع:

measured
synthetic

البروتوكول يحتوي:

minMeasuredCases
maxMapePct
minBenefitVehHours
maxTransitBreaches
maxCriticalAccessBreaches

النتيجة تحتوي:

status
measuredCases
syntheticCases
maeVehHours
mapePct
signedBiasVehHours
totalObservedBenefitVehHours
violations

القيم المسموحة للحالة:

insufficient-evidence
pass
fail

قواعد لا تفاوض عليها:

- الحالات التركيبية لا تدخل في مقاييس النجاح أو الفشل.
- لا نجاح قبل بلوغ الحد الأدنى للحالات المقاسة.
- أي تجاوز لوصول الطوارئ يفشل البروتوكول.
- منفعة الحالة تساوي خطها المضاد ناقص مشاهدتها.
- الخطأ يساوي التنبؤ ناقص المشاهدة.
- لا تحسب نسبة خطأ عندما تكون القيمة المشاهدة صفراً؛ أعد مخالفة بيانات بدلاً من قيمة لا نهائية.
- الحالة غير الصالحة لا تختفي من الحساب بصمت.

اكتب الاختبارات أولاً في:

presentation\tests\shadow-evaluation-test.js

استخدم الهيكل:

const assert = require('node:assert');
const Shadow = require('../athar-shadow-evaluation.js');

let count = 0;
function test(name, fn) {
  fn();
  count += 1;
  console.log(`  ok - ${name}`);
}

const protocol = {
  minMeasuredCases: 3,
  maxMapePct: 20,
  minBenefitVehHours: 100,
  maxTransitBreaches: 0,
  maxCriticalAccessBreaches: 0
};

test('synthetic cases never satisfy the measured evidence threshold', () => {
  const result = Shadow.evaluateShadowPilot([
    {
      permitId: 'S-1',
      kind: 'synthetic',
      predictedVehHours: 80,
      observedVehHours: 75,
      counterfactualVehHours: 140,
      transitBreach: false,
      criticalAccessBreach: false
    },
    {
      permitId: 'S-2',
      kind: 'synthetic',
      predictedVehHours: 90,
      observedVehHours: 85,
      counterfactualVehHours: 150,
      transitBreach: false,
      criticalAccessBreach: false
    },
    {
      permitId: 'S-3',
      kind: 'synthetic',
      predictedVehHours: 70,
      observedVehHours: 65,
      counterfactualVehHours: 135,
      transitBreach: false,
      criticalAccessBreach: false
    }
  ], protocol);
  assert.strictEqual(result.status, 'insufficient-evidence');
  assert.strictEqual(result.measuredCases, 0);
  assert.strictEqual(result.syntheticCases, 3);
});

test('three measured cases pass only when every threshold passes', () => {
  const result = Shadow.evaluateShadowPilot([
    {
      permitId: 'M-1',
      kind: 'measured',
      predictedVehHours: 90,
      observedVehHours: 100,
      counterfactualVehHours: 150,
      transitBreach: false,
      criticalAccessBreach: false
    },
    {
      permitId: 'M-2',
      kind: 'measured',
      predictedVehHours: 120,
      observedVehHours: 110,
      counterfactualVehHours: 165,
      transitBreach: false,
      criticalAccessBreach: false
    },
    {
      permitId: 'M-3',
      kind: 'measured',
      predictedVehHours: 85,
      observedVehHours: 80,
      counterfactualVehHours: 140,
      transitBreach: false,
      criticalAccessBreach: false
    }
  ], protocol);
  assert.strictEqual(result.status, 'pass');
  assert.strictEqual(result.measuredCases, 3);
  assert.strictEqual(result.totalObservedBenefitVehHours, 165);
  assert.ok(result.mapePct <= 20);
  assert.deepStrictEqual(result.violations, []);
});

test('critical access breach fails even when numeric thresholds pass', () => {
  const cases = [
    {
      permitId: 'M-1',
      kind: 'measured',
      predictedVehHours: 90,
      observedVehHours: 100,
      counterfactualVehHours: 150,
      transitBreach: false,
      criticalAccessBreach: true
    },
    {
      permitId: 'M-2',
      kind: 'measured',
      predictedVehHours: 120,
      observedVehHours: 110,
      counterfactualVehHours: 165,
      transitBreach: false,
      criticalAccessBreach: false
    },
    {
      permitId: 'M-3',
      kind: 'measured',
      predictedVehHours: 85,
      observedVehHours: 80,
      counterfactualVehHours: 140,
      transitBreach: false,
      criticalAccessBreach: false
    }
  ];
  const result = Shadow.evaluateShadowPilot(cases, protocol);
  assert.strictEqual(result.status, 'fail');
  assert.ok(result.violations.includes('critical-access-breach'));
});

console.log(`ALL SHADOW EVALUATION TESTS PASSED (${count})`);

أضف اختبارات للحالات التالية:

- حالة مقاسة واحدة مع حالات تركيبية كثيرة.
- نسبة خطأ أعلى من الحد.
- منفعة أقل من الحد.
- مخالفة نقل عام.
- قيمة مشاهدة صفرية.
- رقم سالب.
- نوع غير معروف.
- رقم تصريح مكرر.
- بروتوكول ناقص.
- انحياز موجب وسالب.

شغّل الاختبار قبل إنشاء الوحدة. يجب أن يفشل:

node presentation/tests/shadow-evaluation-test.js

نفّذ الوحدة بنمط تصدير يصلح للمتصفح والاختبار:

(function (root, factory) {
  if (typeof module === 'object' && module.exports) {
    module.exports = factory();
  } else {
    root.AtharShadowEvaluation = factory();
  }
})(typeof self !== 'undefined' ? self : this, function () {
  'use strict';

  function validateShadowCases(cases, protocol) {
    const errors = [];
    if (!Array.isArray(cases)) {
      return { valid: false, errors: ['cases-must-be-array'] };
    }
    if (!protocol || typeof protocol !== 'object') {
      return { valid: false, errors: ['protocol-required'] };
    }

    const protocolFields = [
      'minMeasuredCases',
      'maxMapePct',
      'minBenefitVehHours',
      'maxTransitBreaches',
      'maxCriticalAccessBreaches'
    ];
    protocolFields.forEach((field) => {
      if (!Number.isFinite(protocol[field]) || protocol[field] < 0) {
        errors.push(`invalid-protocol-${field}`);
      }
    });

    const seenPermitIds = new Set();
    cases.forEach((item, index) => {
      if (!item || typeof item !== 'object') {
        errors.push(`invalid-case-${index}`);
        return;
      }
      if (!item.permitId) errors.push(`missing-permit-${index}`);
      if (seenPermitIds.has(item.permitId)) {
        errors.push(`duplicate-permit-${item.permitId}`);
      }
      seenPermitIds.add(item.permitId);

      if (!['measured', 'synthetic'].includes(item.kind)) {
        errors.push(`invalid-kind-${index}`);
      }
      [
        'predictedVehHours',
        'observedVehHours',
        'counterfactualVehHours'
      ].forEach((field) => {
        if (!Number.isFinite(item[field]) || item[field] < 0) {
          errors.push(`invalid-${field}-${index}`);
        }
      });
      ['transitBreach', 'criticalAccessBreach'].forEach((field) => {
        if (typeof item[field] !== 'boolean') {
          errors.push(`invalid-${field}-${index}`);
        }
      });
    });

    return { valid: errors.length === 0, errors };
  }

  function evaluateShadowPilot(cases, protocol) {
    const validation = validateShadowCases(cases, protocol);
    if (!validation.valid) {
      return {
        status: 'fail',
        measuredCases: 0,
        syntheticCases: 0,
        maeVehHours: null,
        mapePct: null,
        signedBiasVehHours: null,
        totalObservedBenefitVehHours: null,
        violations: validation.errors
      };
    }

    const measured = cases.filter((item) => item.kind === 'measured');
    const synthetic = cases.filter((item) => item.kind === 'synthetic');
    const violations = [];

    if (measured.length < protocol.minMeasuredCases) {
      violations.push('insufficient-measured-cases');
    }

    const absoluteErrors = measured.map((item) =>
      Math.abs(item.predictedVehHours - item.observedVehHours)
    );
    const signedErrors = measured.map((item) =>
      item.predictedVehHours - item.observedVehHours
    );
    const benefits = measured.map((item) =>
      item.counterfactualVehHours - item.observedVehHours
    );

    const mean = (values) =>
      values.length
        ? values.reduce((sum, value) => sum + value, 0) / values.length
        : null;

    const maeVehHours = mean(absoluteErrors);
    const signedBiasVehHours = mean(signedErrors);
    const totalObservedBenefitVehHours = benefits.length
      ? benefits.reduce((sum, value) => sum + value, 0)
      : null;

    const percentageErrors = measured
      .filter((item) => item.observedVehHours !== 0)
      .map((item) =>
        Math.abs(item.predictedVehHours - item.observedVehHours)
        / Math.abs(item.observedVehHours)
        * 100
      );

    if (percentageErrors.length !== measured.length) {
      violations.push('zero-observed-value');
    }

    const mapePct = mean(percentageErrors);
    const transitBreaches = measured.filter((item) => item.transitBreach).length;
    const criticalAccessBreaches = measured.filter(
      (item) => item.criticalAccessBreach
    ).length;

    if (mapePct !== null && mapePct > protocol.maxMapePct) {
      violations.push('mape-threshold');
    }
    if (
      totalObservedBenefitVehHours !== null
      && totalObservedBenefitVehHours < protocol.minBenefitVehHours
    ) {
      violations.push('benefit-threshold');
    }
    if (transitBreaches > protocol.maxTransitBreaches) {
      violations.push('transit-breach');
    }
    if (criticalAccessBreaches > protocol.maxCriticalAccessBreaches) {
      violations.push('critical-access-breach');
    }

    const insufficient = measured.length < protocol.minMeasuredCases;
    const criticalFailure =
      criticalAccessBreaches > protocol.maxCriticalAccessBreaches;
    return {
      status: criticalFailure
        ? 'fail'
        : insufficient
          ? 'insufficient-evidence'
        : violations.length ? 'fail' : 'pass',
      measuredCases: measured.length,
      syntheticCases: synthetic.length,
      maeVehHours,
      mapePct,
      signedBiasVehHours,
      totalObservedBenefitVehHours,
      violations
    };
  }

  return { evaluateShadowPilot, validateShadowCases };
});

نفّذ الكود السابق ثم اجعله يمر بكل حالات الاختبار المحددة. لا تغيّر عتبات الاختبار لتوافق الناتج، ولا تسمح لقيمة غير صالحة بأن تدخل العمليات الحسابية.

المهمة الثانية: وثيقة بروتوكول ظل قابلة للتطبيق.

أنشئ:

docs\SHADOW-PILOT.md

يجب أن تحدد:

- هدف التجربة.
- العينة الدنيا.
- طريقة اختيار التصاريح بلا انتقاء النتائج.
- البيانات المطلوبة قبل القرار.
- البيانات المرصودة بعد التنفيذ.
- تعريف الخط المضاد.
- نافذة القياس.
- مقاييس الخطأ والانحياز والمنفعة.
- حدود النجاح والفشل.
- وصول الطوارئ والنقل العام كقيود صلبة.
- فصل الحالات المقاسة عن التركيبية.
- مسؤولية اعتماد البيانات.
- سجل تغيير المعايرة.
- قرار الانتقال من الظل إلى توصية بشرية.
- شرط منع الأتمتة الكاملة.

لا تضع تاريخاً أو جهة شريكة وكأنها وافقت. اكتبها كخطة جاهزة للتنفيذ، لا كتجربة تمت.

المهمة الثالثة: اختبار سلامة العرض والادعاءات.

أنشئ:

presentation\tests\pitch-integrity-test.js

يجب أن يقرأ الملفات التي تملكها ويختبر:

- عدد الشرائح الرئيسية ثمان.
- الملاحق موسومة كملاحق ولا تدخل في العداد.
- التوقيت الرئيسي متصل ومجموع مدده مئة وثمانون ثانية.
- كل بطاقة رقمية لها مرجع محلي أو رابط مباشر.
- لا توجد ادعاءات السبق العامة.
- لا توجد الأرقام السنوية غير المدعومة.
- لا يوجد ادعاء تكامل مباشر منفذ مع بلدي.
- لا يوجد وصف لخوارزمية مستعمرة النمل.
- رابط نسبة الحفر المشترك يصل إلى المصدر الأولي.
- رابط حجم أعمال الطرق يصل إلى صفحة رسمية تذكر الرقم مباشرة.

أنشئ قائمة منع مركزية في الاختبار:

const forbiddenClaims = [
  'أول أداة',
  'الأولى من نوعها',
  '10,000,000',
  '10 million',
  '20,800',
  'ACO',
  'Ant Colony Optimization',
  'يعمل مباشرة على بيانات بلدي دون بنية إضافية'
];

لا تجعل الاختبار يعتمد على صياغة عربية واحدة فقط. أضف الصيغ الفعلية الموجودة في الملفات بعد جردها.

شغّل الاختبار قبل تعديل العرض. يجب أن يفشل:

node presentation/tests/pitch-integrity-test.js

المهمة الرابعة: إعادة بناء القصة حول قرار واحد.

القصة الرئيسية ثماني شرائح وثلاث دقائق:

١. مشكلة تصريح واحد وتأثيره الشبكي.
٢. لماذا الفحص الحالي لا يكفي وحده.
٣. بيانات القرار وما هو حقيقي وما هو افتراض.
٤. مقارنة خط الأساس ببديل زمني واحد.
٥. مسار تحويل واحد وما يحده.
٦. الأثر بنطاق، لا برقم نقطي متضخم.
٧. بروتوكول الظل ومعيار الفشل.
٨. الطلب المحدد لتجربة ظل بشرية الإشراف.

لا تجعل العرض جولة لكل وحدة. اختر حالة واحدة، واجعل كل شاشة تجيب سؤالاً واحداً.

قواعد الأرقام:

- الحالة التوضيحية تسمى توضيحية في الشريحة نفسها.
- البيانات الرسمية تسمى رسمية مع الجهة والتاريخ.
- المشتق يعرض المعادلة أو رابط سجل الشفافية.
- الافتراض يظهر بوسمه.
- لا تعرض أثراً سنوياً للمدينة.
- لا تعرض وفراً ميدانياً.
- لا تحول النطاق إلى قيمة وسطى بلا مبرر.

قواعد المصادر:

- رابط الرقم يجب أن يفتح الدليل المباشر، لا صفحة سجل عامة.
- المصدر المحلي له معرف ثابت.
- المعرف نفسه يستخدم في الشرائح الثلاث.
- لا تكرر نسخاً مختلفة من الرقم في ملفات العرض.

عدّل الملفات الثلاثة بحيث تتطابق القصة والأرقام:

presentation\athar-pitch.html
presentation\athar.html
presentation\athar-merged.html

اجعل العداد:

8

وافصل الملحق بهذه الصيغة المفاهيمية:

Appendix A
Appendix B

لا تدخل الملاحق في زمن العرض الرئيسي.

المهمة الخامسة: مزامنة سجل المصادر وبطاقة الفكرة.

في:

presentation\athar-sources.html

أصلح الروابط المباشرة، وأضف تاريخ الوصول، وميّز بين المصدر الأولي والثانوي. احتفظ بالمصدر الثانوي فقط كشرح إذا كانت له فائدة، لكن لا تجعله سند الرقم الأساسي.

في:

بطاقة-الفكرة.md

احذف أو أعد صياغة:

- ادعاء أول أداة.
- الأثر السنوي غير المدعوم.
- التكامل المباشر غير المنفذ.
- وصف خوارزمية مستعمرة النمل.

استبدلها بالصياغات الصادقة التالية من حيث المعنى:

- الفجوة المقترحة هي حزمة قرار كمية قبل التصريح، وليست ادعاء سبق مطلقاً.
- الحالة الحالية توضيحية وتحتاج تجربة ظل قبل أي اعتماد للأثر.
- التكامل مع أنظمة البلدية مسار نشر مقترح، وليس تكاملاً منفذاً.
- المحسن الحالي بحث محدود وشفاف بين بدائل معرفة مسبقاً.

حقول الفريق:

لا تخترع أسماء أو أدواراً. اتركها موسومة بوضوح كمانع تسليم خارجي في التقرير، من دون سؤال المستخدم ومن دون إخفائها.

المهمة السادسة: تحقق العرض.

شغّل العرض الرئيسي في المتصفح وتحقق من:

- الترتيب من واحد إلى ثمانية.
- عدم احتساب الملحق.
- بقاء كل شريحة ضمن مساحة الشاشة الشائعة.
- وضوح الأرقام والمصادر.
- صلاحية التنقل بلوحة المفاتيح.
- عمل الروابط المحلية.
- عدم وجود أخطاء في وحدة التحكم.
- إمكانية تقديم القصة في ثلاث دقائق من دون فتح الملحق.

نفّذ بروفة نصية فعلية مع مؤقت وسجّل الزمن في التقرير. إذا تجاوزت القصة مئة وثمانين ثانية، اختصر النص؛ لا تسرع الإلقاء افتراضياً.

دورات الالتزام:

الدورة الأولى:

git add presentation/athar-shadow-evaluation.js presentation/tests/shadow-evaluation-test.js docs/SHADOW-PILOT.md
git commit -m "feat: add evidence-gated shadow impact evaluation"

الدورة الثانية:

git add presentation/tests/pitch-integrity-test.js presentation/athar-sources.html بطاقة-الفكرة.md
git commit -m "docs: bind impact claims to primary evidence"

الدورة الثالثة:

git add presentation/athar-pitch.html presentation/athar.html presentation/athar-merged.html docs/REPORT-crit46-final.md
git commit -m "docs: align three-minute pitch with measured evidence"

قبل كل التزام شغّل:

node presentation/tests/engine-test.js
node presentation/tests/ui-smoke-test.js
node presentation/tests/routing-test.js
node presentation/tests/innovation-test.js
node presentation/tests/shadow-evaluation-test.js
node presentation/tests/pitch-integrity-test.js

أنشئ التقرير النهائي في:

docs\REPORT-crit46-final.md

يجب أن يحتوي:

- جرد الادعاءات قبل التعديل.
- تصنيف كل رقم.
- المصدر الأولي المباشر لكل رقم باق.
- الادعاءات التي حذفت وسبب حذفها.
- معادلات تقييم الظل.
- اختبار عددي ناجح وآخر فاشل وآخر ناقص الأدلة.
- نتائج جميع الاختبارات بالأعداد.
- نتيجة التحقق في المتصفح.
- زمن البروفة.
- مانع بيانات الفريق.
- العيوب الباقية.
- تقييم بارد للأثر والاستدامة من عشرة.
- تقييم بارد للعرض التقديمي من عشرة.

لا تقيّم المعايير الأربعة الأخرى.

بوابة القبول:

- لا نجاح من حالات تركيبية.
- لا نجاح قبل الحد الأدنى المقاس.
- وصول الطوارئ قيد فشل صلب.
- كل رقم باق له مصدر مباشر أو معادلة.
- لا أثر سنوي غير مدعوم.
- لا ادعاء سبق.
- لا ادعاء تكامل غير منفذ.
- لا خوارزمية غير موجودة.
- ثماني شرائح رئيسية فقط.
- زمن القصة لا يتجاوز ثلاث دقائق.
- الملاحق خارج العداد والزمن.
- العرض والملف المجمع وبطاقة الفكرة متسقة.
- جميع الاختبارات خضراء.
- لا أخطاء في المتصفح.
- لا تعديل خارج الملكية.

صيغة ردك النهائي:

ابدأ بالحكم، ثم الالتزامات، ثم أعداد الاختبارات، ثم رابط التقرير، ثم الدرجتين الباردتين، ثم العيوب الباقية. لا تطلب مني أي خطوة لاحقة.
```

## قائمة تحقق الوكيل

- [ ] جرد الادعاءات وتصنيفها.
- [ ] فتح المصادر الأولية وتحديث الروابط المباشرة.
- [ ] كتابة اختبارات تقييم الظل قبل التنفيذ.
- [ ] تنفيذ حالات النجاح والفشل ونقص الأدلة.
- [ ] كتابة بروتوكول التجربة الظلية.
- [ ] إضافة اختبار سلامة العرض.
- [ ] توحيد القصة عبر ملفات العرض.
- [ ] تصحيح بطاقة الفكرة.
- [ ] تنفيذ بروفة ثلاث دقائق.
- [ ] التحقق في المتصفح.
- [ ] تشغيل جميع الاختبارات.
- [ ] كتابة التقرير والتقييم لمعيارين فقط.
