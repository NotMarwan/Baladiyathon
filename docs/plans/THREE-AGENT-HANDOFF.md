# تسليم ثلاثة وكلاء — أثر · 26 يوليو 2026

**الأساس:** `main` عند `904a149` · 72 حزمة · 1,198 فحصاً · شجرة نظيفة
**الحكم الحالي:** 7.6 / 10 عبر المعايير الستة
**الموعد:** الهاكاثون 27–28 يوليو

---

## لماذا هذا التقسيم بالذات

الترجيحان — المتساوي والمائل للعرض — أعطيا النتيجة نفسها تقريباً. سبب ذلك
واحد: **المنتج تقدّم أكثر من عرضه**. فأعلى عائد ليس في شيفرة جديدة.

والتقسيم مبنيّ على **ملكية ملفات** لا على مواضيع، كي لا يتصادم الثلاثة:

| الوكيل | يملك | ممنوع عليه |
|---|---|---|
| **A — العرض** | `output/submission/` · `masar-pitch.html` · `scripts/build-deck-manifest.js` | `masar-engine.js` · `masar-sensitivity.js` · `data/` |
| **B — الدليل المحلي** | `data/` · `scripts/` (عدا build-deck-manifest) · `research/` · `masar-comparable-cases.js` | `masar-engine.js` · `masar-sensitivity.js` · `masar-stability.js` · `output/` |
| **C — الحسم والتحقق** | `masar-engine.js` · `masar-sensitivity.js` · `masar-stability.js` · `tests/` · `docs/` | `data/` · `output/` · `research/` |

### التصادم الوحيد المتوقَّع — وحلّه مكتوب سلفاً

ثلاثتهم سيشغّلون المولِّدات، فتتغيّر عندهم:
`output/submission/deck-manifest.json` · `masar-judging-deck-text.html` ·
`tests/fixtures/test-manifest.json` · `tests/fixtures/impact-golden.json`

**قاعدة الدمج:** هذه الملفات **مولَّدة**. عند التعارض لا تُدمج يدوياً —
خُذ أي جانب ثم أعد التوليد:

```bash
node presentation/scripts/build-city-portfolio.js
node presentation/scripts/build-impact-golden.js
node presentation/scripts/build-wzdx-conformance.js
node presentation/scripts/build-alternate-load.js
node presentation/scripts/build-stability-report.js
node presentation/scripts/build-interop-report.js
node presentation/scripts/build-deck-manifest.js
node presentation/scripts/build-catalog-pages.js
npm test
```

**ترتيب الدمج المقترح:** C ثم B ثم A. (C يغيّر المحرك، B يغيّر البيانات،
A يقرأ الأرقام ولا ينتجها — فيُدمج أخيراً على أرقام مستقرة.)

---

## تجهيز أشجار العمل

```bash
cd /c/Users/wasan/Downloads/Swarm/Baladiyathon
git worktree add ../masar-A-deck    -b agent/a-deck    main
git worktree add ../masar-B-evidence -b agent/b-evidence main
git worktree add ../masar-C-decide  -b agent/c-decide  main
```

ثم في كل شجرة — **إلزامي**، وإلا سقط المحقق الرسمي:

```bash
npm install
node presentation/tests/run-all.js
```

يجب أن يقول `72/72 حزمة نجحت · 1198 فحصاً` قبل أي تعديل.

---

## قواعد تسري على الثلاثة

انسخها في رأس كل برومبت.

```
## اقرأ أولاً (إلزامي، قبل أي تعديل)
1. docs/audits/final-independent-acceptance/GO-NO-GO.md
2. docs/audits/final-independent-acceptance/REMEDIATION-DELTA.md
3. docs/audits/final-independent-acceptance/SIX-CRITERIA-COLD-JUDGING.md
4. presentation/masar-canonical.js — BANNED_CLAIMS

## قواعد لا تُخرق
- لا تُرخِ عتبة ولا تضيّق نطاقاً ولا تغيّر وزناً لتحسين رقم معروض.
  الهشاشة المعلنة أصدق من استقرار مصطنع. إن اتّسع الامتناع بعد عملك فتلك
  نتيجة صحيحة لا انحدار.
- لا تعدّل حزمة اختبار لتمرّ. إن سقطت فإمّا الشيفرة خطأ أو الحزمة تصف عيباً.
  الاستثناء الوحيد: حزمة تثبّت عبارة صارت غير صحيحة — تُحدَّث لتثبّت المعنى
  الجديد بدقة، ولا تُحذف. وقد وقع هذا ثلاث مرات في جولة الإصلاح، فتوقّعه.
- لا رقم مكتوب يدوياً في أي سطح. كل رقم يُشتقّ من مصدر مولَّد ويدخل
  deck-manifest.json. رقم خارج الجرد لا يجوز أن يظهر.
- لا ادّعاء أثر ميداني سعودي. الصياغة المسموحة للبيانات الأجنبية:
  «مقيس على N حالة في [بلد]، ولم يُعاير على الرياض».
- أي بيانات خارجية تُحفظ محلياً وتُثبَّت ببصمة SHA-256 وتاريخ سحب.
  المنتج يجب أن يعمل بلا اتصال.
- لا تلمس docs/audits/ — أدلة مجمَّدة. أضف ملفاً جديداً إن لزم.
- التزم بملكية الملفات المحدَّدة لدورك. إن احتجت ملفاً يملكه غيرك، اكتب
  الطلب في ملف تسليم ولا تعدّله.
- بعد كل خطوة: `npm test` أخضر + الشجرة نظيفة بعد إعادة التوليد.

## أبلغ بصدق
إن سقطت خطوة، قل ما سقط وأظهر المخرج الخام. لا تكمل فوق فشل صامت.
إن اتّضح أن بنداً غير قابل للتنفيذ، أعلنه واقفز إلى التالي — وهذا أثمن من
تسليم شيء يبدو منجزاً.
```

---

# الوكيل A — العرض والسرد

**المعيار 6 · الدرجة 6.8 من 10 — أدنى معيار، وأعلاها وزناً حسب المرجع الرسمي**

## التشخيص

العرض المسلَّم 18 شريحة و25 صورة و6,499 محرفاً مرئياً، و**صفر رقم موضوعي
خارج الصور**. عناوينه قوية فعلاً («تمتنع عن الحكم عندما تنقص الحقيقة»)،
والسرد مبنيّ حول الفكرة الصحيحة.

**لكنه متأخّر عن المنتج.** ثلاثة اكتشافات حدثت بعد بنائه ولا يعرفها:

| # | الاكتشاف | لماذا يهمّ |
|---|---|---|
| 1 | **112 من 150 بديلاً لا يتحمّل الحركة المحوَّلة** — 3 فقط تتحمّل | أقوى لحظة إبهار متاحة، وتقلب سردية الأثر من «نوفّر ساعات» إلى «ننقل الازدحام إلى حيّ ما لم يُدار» |
| 2 | **مخطط أثر المثبَّت قبِل 576 منطقة عمل من إدارة نقل ولاية واشنطن بصفر أخطاء** | أقوى دليل جدوى في المستودع: العقد الذي نصدّر عليه هو الذي تنشر عليه جهة حقيقية |
| 3 | **سعة منطقة العمل معايرة على 25 موقعاً مقيساً** بكواشف على شرايين بإشارات | تحوّل معاملاً كان بلا سند إلى مقيس |

## المهمة

1. **شريحتان جديدتان** تحملان الاكتشافين الأول والثاني. الثالث يدخل في
   شريحة موجودة عن النموذج.
2. **راجع كل رقم في العرض** مقابل `output/submission/deck-manifest.json`.
   رقمٌ في شريحة لا يوجد في الجرد **يُحذف أو يُضاف إلى الجرد بمصدره**.
3. **أضف تعداد حمل البديل إلى الجرد** — ثلاثة أرقام على الأقل:
   `alternateOverflows` · `alternateCarries` · `alternateUncomputed`،
   بدرجة `model-derived` وحدّ صريح.
4. **النسخة النصّية تتجدّد تلقائياً** من الجرد — تأكّد أنها تحمل الجديد.
5. **حدّ العرض المعلَن** («أغلب المحتوى داخل صور») يبقى مكتوباً. لا تحذفه
   ولو حسّنت النسبة.

## ما يجعل الشريحة الأولى تعمل

لا تعرض «112» رقماً مجرَّداً. اعرض الحالة الواحدة:

> BLD-2026-0045 — طريق الملك فهد. البديل يحمل **59٪** من طاقته اليوم.
> بعد تحويل **1,760 مركبة/ساعة** إليه يصير **305٪**. يختنق عند شارع السويس.
> **وهذا ليس استثناءً: 112 من 150.**

## تعريف «تمّ»

- `npm test` أخضر · `deck-numbers-test` و`deck-text-test` يمرّان
- كل رقم في العرض له سطر في `deck-manifest.json`
- `git status` نظيفة بعد إعادة توليد الجرد
- لم تُلمس `masar-engine.js` ولا `data/`

---

# الوكيل B — الدليل المحلي: ما هو ممكن فعلاً

**المعياران 3 و4 · 8.5 و6.5 — والأثر الميداني 2.5 من 10**

## اقرأ هذا قبل أن تبدأ — نتيجة سلبية مثبتة

`research/evidence-intelligence/lab/route-match-results.json` يحمل نتيجة
حاسمة أنجزها وكيل سابق:

> عدّادات وزارة النقل (32 محطة، `traffic-density-on-roads.xlsx`) **لا تغطي
> محفظة مسار**. أقرب عدّاد يبعد **14,957 متراً** عن أقرب موقع عمل. صفر
> عدّادات ضمن 500 متر من أي من المسارات الواحد والعشرين.
> والمحطة الوحيدة القريبة من الرياض (`MOT-13`، طريق 513) تحمل علم جودة:
> مجموع الفترة (619) أقل من مجموع الأربع والعشرين ساعة (7,618).

**لا تعِد هذا العمل.** المصنّفات وطنية على طرق سريعة، لا شرايين رياض حضرية.
`aadt` **لا يُغلق منها**، وهذه نتيجة تُعلَن لا فجوة تُملأ.

## ما يبقى ممكناً فعلاً — افحص الثلاثة بهذا الترتيب

### 1. تقاطعات الهيئة الملكية لمدينة الرياض — 631 تقاطعاً حقيقياً

`research/evidence-intelligence/lab/official-data/rcrc-traffic-intersections-2025.json`
يحمل 631 تقاطعاً بإحداثياتها وأسماء شوارعها في الرياض. **بيانات محلية
حقيقية.**

محرك مسار اليوم **يتجاهل الإشارات تماماً**: يستعمل تدفق تشبّع 1800 بلا نسبة
أخضر/دورة. وسعة شريان بإشارات محكومة بالإشارة لا بالمقطع.

**السؤال:** هل يمكن استعمال كثافة التقاطعات على المقطع لتقييد السعة الفعلية
أو لرفع درجة الثقة؟ وإن لم يكن — لماذا، مكتوباً.

**تحذير:** الملف يحمل مواقع التقاطعات لا توقيت إشاراتها. لا تخترع نسبة
أخضر/دورة. إن كان المتاح موقعاً فقط، فأقصى ما يُستخرج مؤشر سياق لا معامل.

### 2. دراسة الدمام — أول حالة سعودية في السجل

`research/evidence-intelligence/lab/papers/dammam-long-term-work-zones.pdf`
— دراسة محكَّمة على **مناطق عمل طويلة الأمد على شرايين في الدمام**،
201 نشاطاً تجارياً في موقعي عمل.

**لكن انتبه:** تقيس الأثر على **الأنشطة التجارية** لا على السعة أو السرعة.
فهي **لا تعاير المحرك**. وقيمتها في مكان آخر: بُعد أثر لا يمثّله أثر
إطلاقاً — تأثير الإغلاق طويل الأمد على الأعمال المجاورة.

**السؤال:** هل تدخل السجل بدرجة `local-comparable`؟ وما الذي تُثبته بالضبط
وما لا تُثبته؟ وهل تستحق بُعداً جديداً في بطاقة القرار («ما لا يقيسه أثر»)؟

### 3. مصادر سعودية مفتوحة أخرى

`SAUDI-DATA-SOURCE-AUDIT.md` في نفس المجلد يحمل ما فُحص. ابحث عمّا لم يُفحص:
بيانات أمانة الرياض المفتوحة، الهيئة العامة للإحصاء، بوابة البيانات الوطنية.
**الشرط: عدّ مروري أو سرعة على شريان حضري في الرياض.** ما دون ذلك سياق لا
معايرة.

## القاعدة الحاكمة لدورك

**النتيجة السلبية المكتوبة أثمن من رقم مُقحَم.** إن انتهيت إلى أن لا شيء
محلياً يعاير المحرك اليوم، فاكتب ذلك بالأدلة — وهذا **إنجاز**، لأنه يحوّل
«لم نبحث» إلى «بحثنا وهذا ما وجدنا»، ويعطي اللجنة طلب بيانات محدَّداً.

## تعريف «تمّ»

- كل مصدر مفحوص له بند في السجل: ما يُثبته، وما لا يُثبته، وتاريخ الوصول
- أي رفع درجة في `comparable-cases.json` يحمل قياساً وطريقة قياس
- `localMeasured` يبقى **صفراً** ما لم توجد حالة سعودية بقياسات قبل/أثناء
- `npm test` أخضر · لم تُلمس `masar-engine.js`

---

# الوكيل C — الحسم والتحقق

**المعياران 1 و5 · 7.5 و8.5 — وثلاثة قرارات معلَّقة**

## المهمة الأولى — قرار الأرضية (الأثقل)

`WORK_ZONE_FRICTION = 1.10` أرضية على نسبة زمن العبور، **بلا سند مقيس**.
كانت موجودة لتمنع تصفير تأخير الليل. وبعد أن صارت سعة منطقة العمل مقيسة
داخل المحرك (1475 مركبة/ساعة/حارة)، **قد تكون وظيفتها انتقلت إلى مكانها
الصحيح**.

قياس مسجَّل: إسقاط الأرضية (`friction = 1.00`) **يقلب 142 توصية من 150**.

**السؤال:** تبقى أم تُسقط؟ وهذا قرار بنيوي مكتوب لا تعديل رقم.

**كيف تحسمه بالدليل لا بالرأي:**
1. احسب نسبة الزمن التي ينتجها BPR وحده عند الطلب المنخفض بعد خفض السعة.
   إن كانت قريبة من 1.0 فالأرضية لا تزال تخترع تأخيراً.
2. ابحث عن سند لأرضية زمنية: تخفيض حدّ السرعة عبر منطقة العمل يعطي نسبة
   زمن مباشرة (`v_free / v_wz`) — وهي **الكمية الصحيحة بُعدياً**.
   حذارِ: `reduced_speed_limit_kph` في تغذية واشنطن **كلها أصفار** (فُحص).
3. إن وُجد سند: أعد تأسيس الأرضية عليه. إن لم يوجد: اكتب القرار وسببه.

**تحذير:** أياً كان القرار سيتغيّر توزيع الامتناع. **لا تُقيَّم النتيجة بعدد
الحالات القابلة للقرار.** سجّل قبل/بعد وفسّره.

## المهمة الثانية — دلتا 89.2٪

`portfolioDeltaPct` ارتفع من 80.8٪ إلى 89.2٪ بعد خفض السعة. الحدّ مكتوب
(«فرق نموذجي — لا وفر مثبت»)، لكن **الرقم كبير بما يستحق فحصاً**: وفرٌ
بتسعة أعشار من إعادة جدولة وحدها غير معقول ميدانياً.

**الفرضية المرشَّحة:** أسّ BPR الرابع مع إزاحة النافذة إلى الليل يجعل الفرق
ينفجر لأن `(v/c)⁴` ينهار عند الطلب المنخفض.

**المطلوب:** فكّك الرقم — كم منه من إزاحة الساعة، وكم من تمديد المدة، وكم
من أسّ المنحنى. وإن كان مبالغاً فيه هيكلياً، **قل ذلك** واقترح عرضه مدىً
أو تقييده. هذا رقم سيُسأل عنه أمام اللجنة.

## المهمة الثالثة — التقطّع في حزم الخادم

`server-test` و`server-security-test` و`city-routing-test` سقطت عابرةً نحو
**3 مرات من 15 تشغيلة كاملة**. تنجح منفردة (8/8) وفي تشغيلات متتالية (3/3).

الفرضية غير المثبتة: ضغط بيئة التدقيق (خادم تطوير ومتصفح متوازيان).

**دليل جديد يضيّق الفرضية:** تكرّر السقوط في **شجرة عمل نظيفة** أُنشئت لتوّها
(`masar-A-deck`) في أول تشغيل بعد `npm install`، **بلا متصفح متصل بها**،
ثم نجح التشغيل الثاني فوراً. أي أن الفرضية «متصفحي كان يستهلك المنافذ»
**ضعيفة**، والأرجح أثر أول تشغيل بارد — ذاكرة مؤقتة، أو تصادم منافذ عابرة
عبر عمليات متوازية، أو مهلة تُتجاوز تحت ضغط أول تحميل. ابدأ من هنا.

**المطلوب:** شغّل الخط عشرين مرة متتالية والتقط **نصّ الفشل الخام** عند
أول سقوط. ثم افحص: استنفاد منافذ عابرة؟ `TIME_WAIT`؟ مهلة زمنية؟ حالة
مشتركة على القرص؟ إما تُصلحه أو **تُوثّق سببه بدليل** — «يُرجَّح» ليست
إجابة في بوابة اختبار.

## المهمة الرابعة — اختبار غير المتخصص (آخر `UNVERIFIED`)

البروتوكول: شخص واحد لم يعمل على مسار · شاشة المكتب على تصريح ممثّل ·
**ستون ثانية بلا شرح شفهي** · ثم الأسئلة السبعة · تسجيل الإجابات **حرفياً**.
النجاح 6 من 7 من داخل الواجهة، وبقسم تفاصيل واحد كحدّ أقصى.

الأسئلة: ما المشكلة؟ · أين الإغلاق؟ · ما الحل المقترح؟ · لماذا اختاره
النظام أو لماذا امتنع؟ · ما أهم خطر؟ · هل النتيجة مؤكدة أم مشروطة؟ · ما
الذي يجب أن يفعله المراجع الآن؟

**النتيجة تُكتب سواء نجح أو سقط** في `docs/NON-EXPERT-TEST-<تاريخ>.md`.
سقوطها **نتيجة صالحة** وأثمن من تجاهلها.

**إن تعذّر إيجاد شخص:** اكتب `UNVERIFIED — no real non-expert participant
was tested` ولا تحاكِ المستخدم ولا تمنح الدرجة.

## تعريف «تمّ»

- قرار الأرضية مكتوب بسببه، وتوزيع الامتناع قبل/بعد مسجَّل
- دلتا 89.2٪ مفكَّكة إلى مكوّناتها، وحكمٌ عليها
- التقطّع مُصلَح أو مُوثَّق بنصّ فشل خام وسبب
- اختبار غير المتخصص مُجرًى ونتيجته مسجَّلة حرفياً — أو `UNVERIFIED` صريحة
- `npm test` أخضر ثلاث مرات متتالية

---

## إن لم يكتمل إلا واحد

**A.** المعيار الأعلى وزناً والأدنى درجة، وأقلّها خطراً (لا يمسّ المحرك)،
وأقصرها زمناً. الشريحتان وحدهما تساويان ما فعله يوم كامل من الشيفرة.
