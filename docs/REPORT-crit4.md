# REPORT — وكيل «claude 1» — المعيار 4: الأثر المتوقع والاستدامة

فرع: `crit4-impact` · دورات الحلقة: 6 (مهمة لكل خصم/تعميق) · اختبارات: 55/55 تنجح · console: صفر أخطاء.

## 1) التغييرات (ملف:سطر)

### `presentation/masar-engine.js`
- حذف `SHARED_TRENCH_OVERHEAD=1.18` (المعاير عكسياً)، أُضيف `DIG_ONCE_SAVED_FRACTION_LOW/HIGH = 0.25/0.33` (~سطر 58).
- `digOnce()` أعيدت كلياً: ترجع نطاق `savedLowSAR/savedHighSAR/savedPctLow/savedPctHigh/sharedLowSAR/sharedHighSAR` مشتق مباشرة من نطاق GAO؛ تصريح واحد = صفر وفر (~سطر 445).
- `WORK_ZONE_FRICTION = 1.10` جديد (~سطر 82)، مطبَّق في `score()`: `closedT = max(bprClosed, baseT*1.10)` (~سطر 148) — يقتل مِراج 99.6%.
- `assumptionsUsed(metric)` + جدول `METRIC_ASSUMPTIONS` جديد (~سطر 528)، مُصدَّر.

### `presentation/masar-impact-calibration.js` (جديد)
- `createCalibration(store)` — سجل back-test في localStorage تحت `masar-backtests-v1`، `correctionFactor()` = وسيط نِسَب observed/predicted، `status()→{n,factor}`، يرفض predicted≤0.

### `presentation/masar-impact-budget.js` (جديد)
- `corridorBudget()` — ميزانية مسار شهرية للمحور، أحكام ضمن/قرب السقف/تجاوز، `remaining` بأرضية 0 لكن `consumedAfter/pctUsed` صادقة.

### `presentation/masar-prototype.html`
- سطر 210: «انبعاثات متجنبة» → «انبعاثات **مضافة** بسبب الإغلاق» (إصلاح الوسم).
- بطاقة CO₂ (خطوة 6): أُزيل الحساب من `delayVehHours`؛ يُملأ الآن في `selectCandidate` من `co2Range(candidate.savedVehHours)` فقط.
- بطاقة Dig-Once: نطاق `savedLow–savedHigh ﷼` + شارة «٢٥–٣٣٪ GAO»؛ «لا دمج» عند غياب تصاريح مجاورة.
- بطاقات optimize: المطلق أولاً «before ← after س-م (وفر X، Y٪)».
- شارات «عدد الافتراضات: N» بجانب قيمة الوقت/CO₂/الحافلات/Dig-Once (مع القائمة على hover).
- جدول الشفافية: +12 صفاً (workZoneFriction, scoreCalibration, minCapacityFraction, compoundFactor, نطاق GAO, الإشغال, الأجر, نصيب VOT, الوقود, الحافلات, الركاب).
- سطر تحميل + بذر 3 حالات معايرة موسومة؛ «التوقع المعايَر: X س-م — معايرة من N حالة»؛ N يكبر فعلياً عند كل اختيار جدولة.
- بطاقة ميزانية المحور + شريط عدّاد ملوّن (أخضر/أصفر/أحمر).

### `presentation/tests/engine-test.js`
- استبدال اختبار digOnce القديم (25–42.5%) باختبارات النطاق الثابت 25/33٪ + تصريح واحد = صفر.
- +اختبارات: أرضية الاحتكاك (ليل≠صفر، savedPct<99، best≥100 س-م)، `assumptionsUsed`، المعايرة (وسيط، رفض، ثبات المخزن)، الميزانية (أحكام، أرضية remaining).

## 2) الدرجة النهائية بالرُبرِك (تقييم عدائي)

| فحص | خصم محتمل | النتيجة | الدليل |
|---|---|---|---|
| إشارة كل رقم متسقة مع وسمه | −3 | 0 | CO₂ «مضافة» من delay، «وفر» من savedVehHours فقط؛ digOnce وفر من الكلفة المنفصلة. لا وسم توفير فوق ضرر. |
| كل نسبة داخل نطاق مصدرها | −2 | 0 | Dig-Once = 25–33٪ حرفياً كاقتباس GAO المجاور (لا 41–60٪). |
| كل رقم نطاق + شارة مصدر + افتراضات معدّة معلنة | −2 | 0 | نطاقات: أشخاص/وقت/CO₂/حافلات/Dig-Once + شارات عدّاد الافتراضات على البطاقة. |
| آلية استدامة فعلية قابلة للعرض | −1.5 | 0 | سجل back-test في localStorage + معامل تصحيح وسيط + N يكبر حياً. ليست ادعاء نصي. |
| الثوابت المعايرة في جدول الشفافية | −1 | 0 | 12 صفاً مضافاً يشمل كل ثابت معاير. |
| تعميق واحد جديد يعمل كاملاً | −0.5 | 0 | ميزانية المحور بعدّاد مرئي + حلقة المعايرة (تعميقان). |

**الدرجة الذاتية العدائية: 9.0/10.**

نقاط الضعف المتبقية المعترف بها (لماذا ليست 10):
- `savedPct` قد يصل ~96٪ لأساس متطرف (إغلاق 48س عبر ذروتين). دافعتُ عنه بالمطلق-أولاً + أرضية الاحتكاك، لكنه لا يزال يبدو مرتفعاً لمحكّم متعجل. −0.5.
- بيانات المعايرة الأولية تركيبية (موسومة). الآلية حقيقية لكن لا حالات ميدانية فعلية بعد. −0.5.

## 3) ما لم أُصلحه ولماذا
- `SCORE_CALIBRATION=0.35` يبقى افتراضياً (لا مصدر) — لكنه الآن معلن في جدول الشفافية وقابل لإعادة المعايرة من السجل. تغييره يمس معيار الدقة (وكيل آخر) لا الأثر.
- لم أمسّ `masar-pitch/merged/masar.html` ولا كود الخريطة/الجدول الزمني (حدود الملكية).

## 4) عدد دورات الحلقة: 6 (+ فحص نهائي). كل الكوميتات في `crit4-impact`.
