# REPORT — فرع `crit35-quality` — المعياران 3 (الجدوى) و5 (النموذج)

المنفذ: كلود المنسق · الدورات: 3 · الكوميتات: `804ecbc` `2920f4c` `8f5d042`

## ما أُغلق (بأدلة)

### المعيار 5 — جودة النموذج (كان 7.0)
| خصم المحكّمين | الحالة | الدليل |
|---|---|---|
| «المرصود» = توقع×1.12 دائماً ⇒ حكم «دقيق» مضمون | ✅ أُغلق | انحراف حتمي متغير 5–34% حسب المرشح؛ تحقق حي: ٥٪ دقيق / ١٢٪ دقيق / **٣١٪ يتطلب إعادة معايرة** + وسم «حكم على حالة تركيبية» على الحكم نفسه |
| إحداثيات WZDx ثابتة لا تتبع الاختيار | ✅ أُغلق | التصدير يقرأ `CORRIDOR_COORDS[state.selectedSegIdx]` ويقلب lat/lng لصيغة GeoJSON |
| اختبار العتبات تحصيل حاصل (لا يستدعي score) | ✅ أُغلق | الاختبار يستدعي `score()` الحقيقية عبر 5 شدّات ويطالب بمستويين مختلفين على الأقل |
| صفر اختبارات واجهة | ✅ أُغلق | `tests/ui-smoke-test.js`: 9 فحوص (IDs مربوطة، فخ عكس الإشارة، وسوم التركيبي، ثوابت المعايرة، تطابق لقطة RCRC) |
| 4 ثوابت معايرة غائبة عن جدول الشفافية | ✅ أُغلق | `CALIBRATION` مُصدَّر من المحرك + 4 صفوف جديدة في ASSUMPTIONS تقرأ القيم ديناميكياً (تبقى صحيحة بعد إعادة معايرة crit4) |
| `trenchKm=1.2` مزروع بلا إعلان | ✅ مُعلن | صف ASSUMPTIONS «طول الخندق في حساب Dig-Once» |
| «تصريحين متجاورين» يوحي بموقع مستقل | ✅ وُسم | «تقدير تقريبي: يفترض تصريحاً مماثلاً على المحور نفسه» |
| `lengthKm` معروض كأنه يغذي الحساب | ✅ وُسم | «معلوماتي فقط: لا يدخل في حساب BPR» |
| هشاشة regex العربي لاستخراج النافذة | ✅ أُغلق | حقول مهيكلة (`phases`/`nights` + `WORK_WINDOW_HOURS` المُصدَّر) والregex fallback فقط |
| وسم `capacityPerLane` «مصدر» خطأً | ✅ صُحح | `tag: 'assume'` |

### المعيار 3 — الجدوى (كان 7.0)
| خصم المحكّمين | الحالة | الدليل |
|---|---|---|
| «لا مصدر بيانات حقيقي مؤمَّن» | ✅ جزئي-قوي | **بيانات RCRC حقيقية جُلبت من الـAPI الحي وسُلكت**: 9 مسارات حافلات فعلية + 70 محطة + 6 محطات مترو ضمن 2كم من الممر — `data/rcrc-corridor-transit.json` ببيان منشأ كامل (استعلام، تاريخ، ترخيص، src-010/011) + اختبار يضمن تطابق اللقطة المضمنة مع الملف. `transitImpact` يستهلك العدد الحقيقي (36 حافلة/ساعة بدل 12 المفترضة) |
| معمارية الدرجات وخطة تجربة غير موثقة | ✅ أُغلق | `docs/FEASIBILITY.md`: 3 درجات + وضع الظل (20–30 تصريحاً، 4–8 أسابيع، تثبيت المنهج مسبقاً) + قالب طلب البيانات |
| AADT/السرعات غير مؤمنة | ⚠️ متبقٍ بحدود | لا مصدر عام منشور (فحصت كتالوج RCRC — لا بيانات عدّ مروري). موثق كافتراض + مسار الحصول في FEASIBILITY. غير قابل للإغلاق قبل شراكة جهة |

## التقييم الذاتي الصارم

- **المعيار 5 — نطاق مسؤوليتي: 9.5** (المتبقي خارج نطاقي: المسار البديل الوهمي = فرع crit1، وعرض Dig-Once المتناقض = فرع crit4).
- **المعيار 3 — نطاق مسؤوليتي: 9.0** (المتبقي: AADT بشراكة جهة فقط — موثق بصدق؛ وادعاءا Balady API وSUMO في ملفات فرع crit6).
- **درجة المعيارين كاملين لن تُعتمد إلا بعد دمج الفروع الأخرى** — الاعتماد النهائي بإعادة تقييم عدائية بعد الدمج، لا بهذا التقرير.

## فحوص الإغلاق النهائية

`node presentation/tests/engine-test.js` ⇒ 47/47 · `node presentation/tests/ui-smoke-test.js` ⇒ 9/9 · متصفح `file://`: التدفق كامل، جدول شفافية 15 صفاً، صفر أخطاء console.
