# تعليمات الوكلاء — مشروع مسار

> يحلّ هذا الملف محل `agent1..4/AGENTS.md` الأربعة. تلك حملت مسارات مكسورة ونُقلت إلى `_archive/agent-briefs/`. هذا الملف هو المرجع الوحيد.

## المشروع

**مسار (Masar)** — بلدياثون ٢٠٢٦، التحدي الثالث: تقييم أثر الحفريات على الحركة قبل إصدار التصريح. كل ما عداه منتهٍ ومؤرشف.

## اقرأ بهذا الترتيب

1. [`README.md`](README.md) — الهيكل وقواعد التنقل. **جدول «وكيل جديد؟» في أعلاه هو نقطة البداية**
2. `graphify query "<سؤالك>"` — حدّد الملفات ذات الصلة قبل فتح أي شيء
3. `SwarmingObsidian/01 Masar/Masar - Master.md` — المواصفة
4. `presentation/README-masar.md` — بنية النموذج وصفحاته الخمس

المسارات كلها نسبية من جذر المستودع. لا تستخدم مسارات مطلقة تبدأ بـ `C:/Users/...` — هي أول ما ينكسر عند نسخ المستودع أو العمل في worktree. (هذا بالضبط ما كسر البريفات الأربعة القديمة في `_archive/agent-briefs/`.)

## قبل أول قياس في شجرة عمل جديدة

```bash
cp -r Baladiyathon/node_modules <worktree>/node_modules
```

بدونها تسقط `wzdx-official-schema-test.js` و`measurement-pack-test.js` بـ `Cannot find module 'ajv'`، فتقرأ «75/77» وتظنّه خطّ أساسك. الصحيح **77/77 · 1278 فحصاً**.

وإن سقطت `server-test` أو `server-security-test` أو `city-routing-test`: تقطّع معروف بنحو ١٠٪ من التشغيلات، موثّق في `docs/engineering/FLAKY-SERVER-SUITES-2026-07-26.md`. أعد التشغيل قبل أن تظنّه منك.

## إن فوّضت وكيلاً فرعياً

أعطه **مسارات مطلقة** وحدّد صراحةً ما لا يلمسه، واطلب تأكيد موقعه قبل أي حذف. وكيل حذف `_archive` في الشجرة الرئيسية بدل شجرته المعزولة لأن التعليمات اعتمدت مساراً نسبياً. راجع أثره بـ `git status` بعد انتهائه.

## ممنوعات

| ممنوع | لماذا |
|---|---|
| قراءة `_archive/` | محتوى ميت — يُنتج قرارات مبنية على واقع منتهٍ |
| إعادة اشتقاق معادلة مرورية خارج `presentation/masar-engine.js` | مصدر الحساب واحد، والاختبارات والواجهة تشتركان فيه |
| إضافة رقم بلا تصنيف مصدر | التحكيم يخصم على كل ادعاء بلا دليل |
| ترك `npm test` أحمر | ٧٧ حزمة · ١٢٧٨ فحصاً هي بوابة القبول |
| ملف جديد في جذر المستودع | الجذر يحمل أربعة ملفات متتبَّعة فقط. ضع الملف في `docs/` أو `tools/` |

## تصنيف الأدلة

كل رقم يحمل واحداً من أربعة: **مُثبَت عملياً** · **مُتحقَّق خارجياً** · **متوقَّع من تجربة** · **سيناريو توسّع مشروط**. لا تقدّم افتراض الـ ١٠٬٠٠٠ تصريح الوطني على أنه رقم رسمي.

## قبل أن تسلّم

```bash
npm test
```

أخضر أم لا تسلّم. وإن نقلت ملفاً يشير إليه كود أو اختبار، حدّث المرجع في نفس الالتزام.
